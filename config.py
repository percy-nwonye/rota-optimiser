# config.py
WARNINGS_ENABLED = True          # show “this breaks a rule” prompts
ALLOW_OVERRIDES = True           # allow manager to override at all
PROMPT_FOR_OVERRIDES = True      # ask y/n when a rule is broken
